{% extends 'base.html.twig' %}

{% block title %}Create Equipment
{% endblock %}

{% block body %}
	<style>
		.btn-floating {
			border-radius: 45%;
			color: #fff;
margin:5px
		}
        .form-outline{
            margin:40px;
            color:rgb(0, 11, 216);
        }
	</style>

<div class="container mt-4">
    <h1>Create Equipment</h1>
    <form method="post" action="{{ path('create_equipment') }}">
        <div class="mb-3">
            <label class="form-label">Equipment Name</label>
            <input type="text" class="form-control" name="equipment">
        </div>
        <div class="mb-3">
            <label class="form-label">Description</label>
            <input type="text" class="form-control" name="description">
        </div>
        <button type="submit" class="btn btn-primary">Submit</button>
    </form>
</div>
	<div class=" container mt-4">
		<h1>Liste d'équipements</h1>
<div class="form-outline" data-mdb-input-init>
  <label class="form-label" for="form1">Search equipment</label>
    <input type="search" id="form1" class="form-control" placeholder="Equipment" aria-label="Search" />
  
</div>

		<table class="table align-middle mb-0 bg-white">
			<thead class="bg-light">
				<tr>
					<th>Name Equipement</th>
					<th>Description</th>
					<th>Status</th>
					<th>Affectation</th>
					<th>Actions</th>
				</tr>
			</thead>
			<tbody>
			  {% for equipment in equipments %}
				<tr>
					<td>
						<div class="d-flex align-items-center">

							<div class="ms-3">
								<p class="fw-bold mb-1">{{ equipment.getName() }}</p>
								<p class="text-muted mb-0">Monsieur Nizar</p>
							</div>
						</div>
					</td>
					<td>
						<p class="fw-normal mb-1">{{ equipment.getDescription() }}</p>
						<p class="text-muted mb-0">IT department</p>
					</td>
					<td>
						<button type="button" class="btn btn-outline-success btn-rounded" data-mdb-ripple-init data-mdb-ripple-color="dark">enable</button>
					</td>
					<td>
						<select class="form-select" aria-label="Default select example">
							<option selected>Select project</option>
							<option value="1">One</option>
							<option value="2">Two</option>
							<option value="3">Three</option>
						</select>
					</td>
					<td>
						<button type="button" class="btn btn-danger btn-floating" data-mdb-ripple-init data-mdb-ripple-color="dark">
							<i class="fas fa-ban"></i>

						</button>
						<button type="button" class="btn btn-info btn-floating" data-mdb-ripple-init>
							<i class="fas fa-magic"></i>
						</button>
					</td>
				</tr>
				  {% endfor %}

			</tbody>
		</table>
	</div>

{% endblock %}
